# 🔧 Руководство по исправлению стилей Tailwind CSS

## ✅ Что было исправлено

Проблема заключалась в том, что использовался **Tailwind CSS v4.0** (бета-версия), который имеет совершенно другую архитектуру и не полностью стабилен. 

### Изменения:

1. **Откатились на Tailwind CSS v3.4** (стабильная версия)
   - Удален пакет `@tailwindcss/postcss`
   - Добавлен `autoprefixer`
   - Изменена версия `tailwindcss` с `^4.0.0` на `^3.4.0`

2. **Обновлен `globals.css`**
   - Заменен `@import "tailwindcss"` на стандартные директивы v3:
     ```css
     @tailwind base;
     @tailwind components;
     @tailwind utilities;
     ```
   - Удален специфичный для v4 блок `@theme inline`
   - Удалена директива `@custom-variant` (специфична для v4)

3. **Обновлен `postcss.config.js`**
   ```js
   module.exports = {
     plugins: {
       tailwindcss: {},
       autoprefixer: {},
     },
   }
   ```

4. **Создан `tailwind.config.js`** для v3
   - Указаны пути к контенту
   - Настроены кастомные цвета и расширения темы

5. **Обновлен `vite.config.ts`**
   - Добавлена явная настройка `css.postcss`

## 📋 Шаги для пересборки

### 1. Удалите старые зависимости и кеш
```bash
rm -rf node_modules
rm -rf dist
rm package-lock.json
```

### 2. Установите обновленные зависимости
```bash
npm install
```

### 3. Пересоберите проект
```bash
npm run build
```

### 4. (Опционально) Протестируйте локально
```bash
npm run preview
```
Откройте http://localhost:4173 и проверьте, что стили загружаются.

### 5. Задеплойте на Netlify

**Вариант A - Netlify Drop (быстро):**
1. Откройте https://app.netlify.com/drop
2. Перетащите папку `dist` в окно браузера
3. Скопируйте новый URL
4. В настройках Netlify привяжите домен weddingtravel.by к новому сайту

**Вариант B - Git Deploy (автоматически):**
```bash
git add .
git commit -m "Fix: Migrate from Tailwind v4 to v3 to resolve styling issues"
git push origin main
```

Если у вас настроен автодеплой, Netlify автоматически пересоберет проект.

**Вариант C - Netlify CLI:**
```bash
npm install -g netlify-cli
netlify login
netlify deploy --prod --dir=dist
```

## 🎯 Проверка результата

После деплоя:
1. Откройте https://www.weddingtravel.by/
2. Нажмите **Ctrl+Shift+R** (полная перезагрузка с очисткой кеша)
3. Проверьте, что:
   - Hero-секция отображается с правильными стилями
   - Хедер имеет навигацию и корректный фон
   - Кнопки стилизованы
   - Сетки и отступы работают корректно

## 🔍 Как проверить стили в DevTools

1. Откройте Chrome DevTools (F12)
2. Перейдите на вкладку **Network**
3. Обновите страницу
4. Найдите файл `.css` (например, `index-abc123.css`)
5. Откройте его и проверьте, что там есть классы Tailwind:
   - `.flex`, `.grid`, `.bg-white`, `.text-center` и т.д.

Если классы есть - стили работают! ✅

## ⚠️ Важные моменты

- **Tailwind CSS v4** все еще в разработке и может иметь баги
- **Tailwind CSS v3.4** - стабильная, производственная версия
- Все функции остались теми же, просто изменилась конфигурация
- Никаких изменений в React компонентах не требуется

## 📞 Если что-то не работает

1. Проверьте, что `node_modules` полностью переустановлены
2. Проверьте, что `dist` папка пересоздана
3. Проверьте логи сборки на ошибки: `npm run build 2>&1 | tee build.log`
4. Убедитесь, что кеш браузера очищен (Ctrl+Shift+R)

Удачи! 🚀
