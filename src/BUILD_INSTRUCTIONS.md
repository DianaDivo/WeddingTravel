# 🚀 Инструкции по сборке проекта

## ✅ Что было исправлено

Проект теперь содержит все необходимые файлы для работы с Vite:

- ✅ `index.html` - главный HTML файл
- ✅ `main.tsx` - точка входа React приложения
- ✅ `vite.config.ts` - конфигурация Vite
- ✅ `public/favicon.svg` - иконка сайта
- ✅ Добавлены зависимости: `react-router-dom`, `@supabase/supabase-js`

## 📦 Шаги для сборки

### 1. Установите зависимости

```bash
npm install
```

⏱️ Подождите 1-3 минуты...

### 2. (Опционально) Создайте .env файл

```bash
cp .env.example .env
```

Затем отредактируйте `.env` и добавьте ваши данные Supabase:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-anon-key
```

### 3. Соберите проект

```bash
npm run build
```

⏱️ Подождите 30-60 секунд...

### 4. Проверьте результат

Должна создаться папка `dist/`:

```bash
ls -la dist/
```

## 🎉 Готово к деплою!

Теперь папку `dist` можно загрузить на:

- **Netlify Drop**: https://app.netlify.com/drop
- **Vercel**: `vercel --prod`
- Любой другой хостинг

## 🔍 Локальный просмотр

Чтобы посмотреть собранный сайт локально:

```bash
npm run preview
```

Откройте: http://localhost:4173

## 🛠️ Разработка

Для запуска dev сервера:

```bash
npm run dev
```

Откройте: http://localhost:5173

## ❗ Важные примечания

1. **Файл `.env` не коммитится в Git** (он в `.gitignore`)
2. **Перед деплоем на продакшн** - укажите реальные ключи Supabase
3. **Папка `dist`** - это готовый сайт для загрузки на хостинг

## 📞 Нужна помощь?

Если возникли проблемы:

1. Убедитесь что Node.js версии 18+ (`node --version`)
2. Удалите `node_modules` и установите заново:
   ```bash
   rm -rf node_modules
   npm install
   ```
3. Проверьте что все файлы на месте (index.html, main.tsx, vite.config.ts)